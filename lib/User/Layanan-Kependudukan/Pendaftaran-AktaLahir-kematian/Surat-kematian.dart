import 'package:flutter/material.dart';

class SuratKematian extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Surat Kematian'),
      ),
      body: Center(
        child: Text('Hello World'),
      ),
    );
  }
}
