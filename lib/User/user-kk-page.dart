import 'package:flutter/material.dart';

class UserKkPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('KK Page'),
      ),
      body: Center(
        child: Text('Hello World from KK Page!'),
      ),
    );
  }
}
