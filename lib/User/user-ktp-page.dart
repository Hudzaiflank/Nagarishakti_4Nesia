import 'package:flutter/material.dart';

class UserKtpPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('KTP Page'),
      ),
      body: Center(
        child: Text('Hello World from KTP Page!'),
      ),
    );
  }
}
